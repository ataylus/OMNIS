# Runtime: the engine, report, and dashboard.
pandas==2.3.3
pydantic==2.13.4
numpy==2.4.3
fpdf2==2.8.7        # PDF report; pulls fonttools, used to embed Latin Modern.

# Tests.
pytest==8.4.2

# Notebook only (not needed to run the engine, the CLI, or the dashboard).
matplotlib==3.10.8
